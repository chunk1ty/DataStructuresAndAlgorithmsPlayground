namespace Algorithms.Combinatorics
{
    public class CombinatoricPlaygrounder
    {
        public static void Play()
        {
            // Permutator.Permute(new[] {'A', 'B', 'C'});

            Combinator.Combinate(items: new[] {'a', 'b', 'c', 'd'}, p: 2);
        }
    }
}